{
  "name": "getpdfpro",
  "version": "0.1.0",
  "private": true,
  "description": "GetPDFPro — the professional PDF toolkit. Free, fast, private.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "2.9.14",
    "typescript": "5.6.3",
    "prettier": "3.3.3",
    "prettier-plugin-tailwindcss": "0.6.8"
  }
}
