# Web framework
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# PDF processing
PyMuPDF==1.24.13
pillow==11.0.0
pikepdf==9.4.0
reportlab==4.2.5

# OCR (Tesseract via pytesseract)
# The Tesseract binary itself is installed in the Dockerfile (apt).
# pytesseract is the Python binding. Tesseract must be reachable at
# /usr/bin/tesseract (default on python:3.12-slim after apt install).
pytesseract==0.3.13

# Office document generation
# python-docx is the .docx writer. We use it for PDF -> Word.
# The conversion is best-effort: text + basic structure (headings,
# paragraphs, tables). Complex layouts (multi-column, math, complex
# tables) will NOT round-trip faithfully — we mark this clearly in
# the UI and API responses.
python-docx==1.1.2

# HTML to PDF rendering (used by the /tools/html-to-pdf tool).
# xhtml2pdf is pure-Python (no native deps), works in slim Docker.
# Fidelity ~80% vs WeasyPrint's 95% but avoids the 50MB pango/cairo
# install. Supports CSS2.1 + a subset of CSS3.
xhtml2pdf==0.2.16

# AI
google-generativeai==0.8.3
httpx==0.27.2

# Config & validation
pydantic==2.9.2
pydantic-settings==2.6.1

# Database & storage
supabase==2.8.1
boto3==1.35.54  # Cloudflare R2 (S3-compatible)

# TOTP for the Supabase MFA verification harness
# (scripts/verify_supabase_mfa.py and its tests).
# Pure-Python, no native deps.
pyotp==2.9.0

# Queue & tasks
celery==5.4.0
redis==5.2.0

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Observability
sentry-sdk[fastapi]==2.18.0
structlog==24.4.0

# Utils
python-dotenv==1.0.1
tenacity==9.0.0

# Billing (lazy-imported in app/routers/billing.py)
stripe==11.0.0
