# Pytest config for the canonical `apps/api` tree.
#
# We keep the config minimal — most settings come from pyproject.toml
# when present. The [tool.hatch.build] target in pyproject.toml
# declares the `app` package, and tests live under `tests/` per the
# repo convention.
