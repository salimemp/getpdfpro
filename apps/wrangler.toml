# Cloudflare Workers configuration for the getpdfpro-web app.
# Mirrors apps/web/wrangler.toml — Workers uses .open-next/worker.js + assets binding
# (Pages uses pages_build_output_dir; OpenNext 1.x is Workers-only).

name = "getpdfpro-web"
main = ".open-next/worker.js"
compatibility_date = "2026-04-15"
compatibility_flags = ["nodejs_compat", "global_fetch_strictly_public", "enable_request_signal"]
